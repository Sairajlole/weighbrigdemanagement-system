import 'package:flutter/material.dart';

class WeighmentScreen extends StatelessWidget {
  const WeighmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Weighment — rebuilding'),
    );
  }
}
